import React from 'react';

export interface ProgressDotsProps {
  current: number;
  steps: number;
}

const ProgressDots = ({
  current,
  steps
                      }: ProgressDotsProps) => {
  return <></>
}

export default ProgressDots;