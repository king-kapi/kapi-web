enum Collections {
  USERS = 'users',
  CHATS = 'chats',
  MESSAGES = 'messages',
  POSTS = 'posts',
  LOBBY_JOIN_REQUESTS = 'lobby_join_requests' // what's the naming scheme here?
}

export default Collections;
